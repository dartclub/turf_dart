name: Pull Request Code Coverage Reporting
on:
  workflow_run:
    workflows: ["Dart Unit Tests for PRs"]
    types: [completed]

jobs:
  coverage-reporting:
    if: >
      ${{ github.event.workflow_run.event == 'pull_request' &&
          github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: dart-lang/setup-dart@v1
        with:
          sdk: stable

      - name: Print Dart SDK version
        run: dart --version

      - name: Install dependencies
        run: dart pub get

      - name: Download raw coverage from tests workflow
        uses: dawidd6/action-download-artifact@v19
        with:
          workflow: dart-unit-tests-on-pr.yml
          run_id: ${{ github.event.workflow_run.id }}
          name: raw-coverage
          path: ./artefacts/coverage

      - name: Test artefact download
        run: ls ./artefacts/coverage

      - name: Convert to LCOV report
        run: |
          dart pub global activate coverage
          dart pub global run coverage:format_coverage --packages=.dart_tool/package_config.json --report-on=lib --lcov -o .artefacts/coverage/lcov.info -i .artefacts/coverage

      - name: Generate HTML coverage report
        run: |
          sudo apt install lcov
          genhtml -o .artefacts/coverage/report .artefacts/coverage/lcov.info

      - name: Comment on PR with coverage # NOT CONVINCED THIS WORKS
        continue-on-error: true
        uses: romeovs/lcov-reporter-action@v0.2.21
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          lcov-file: .artefacts/coverage/lcov.info

      - name: Archive coverage report
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: coverage-report
          path: |
            .artefacts/coverage/report
            .artefacts/coverage/lcov.info
